spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/loggingDB"
    username: postgres
    password: ktvano!!

  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    # org.hibernate.type: trace

